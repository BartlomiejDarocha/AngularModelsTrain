export interface DialogData {
    header: string;
    content: string;
  }
export interface NameAndColor {
  name: string;
  color: string;
}
export interface InterceptorError {
  reason: string;
  status: number;
}
